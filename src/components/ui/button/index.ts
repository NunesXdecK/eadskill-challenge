import { Button as Primary } from "./button";
import { Clear } from "./clear";
import { Danger } from "./danger";
import { TrashConfirm } from "./trash-with-confirmation";

export const Button = {
  Clear,
  Danger,
  Primary,
  TrashConfirm,
};
